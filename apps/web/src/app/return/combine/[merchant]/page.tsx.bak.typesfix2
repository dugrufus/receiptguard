import { t } from "@/rg/i18n";

type Params = { merchant: string };

export default function CombineReturnPage({ params }: { params: Params }) {
  return (
    <main className="p-4 space-y-4">
      {/* [RG:BLOCK RETURN.COMBINE START] */}
      <section className="rounded-2xl border p-4">
        <h1 className="text-xl font-semibold">
          {t("return.combine.title") ?? `Combine returns — ${params.merchant}`}
        </h1>
        <div className="text-sm opacity-80"> </div>
      </section>
      {/* [RG:BLOCK RETURN.COMBINE END] */}
    </main>
  );
}
