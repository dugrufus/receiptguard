import { t } from "@/rg/i18n";

type Params = { id: string };

export default function RecallPage({ params }: { params: Params }) {
  return (
    <main className="p-4 space-y-4">
      {/* [RG:BLOCK RECALL.DETAIL START] */}
      <section className="rounded-2xl border p-4">
        <h1 className="text-xl font-semibold">{t("recall.title") ?? "Recall detail"}</h1>
        <div className="text-sm opacity-80">—</div>
        <button type="button" className="rounded-2xl border px-4 h-10">
          {t("recall.cta") ?? "Learn more"}
        </button>
      </section>
      {/* [RG:BLOCK RECALL.DETAIL END] */}
    </main>
  );
}
